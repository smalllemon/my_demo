1、lastIndexof

  用法:返回一个指定的字符串值最后出现的位置，在指定位置从后向前搜索
  
  stringObject.lastIndexOf(searchvalue,fromindex)
  searchvalue:规定要检索的字符串值。
  formIndex:{ 
              意义：指定的位置;
              取值范围为：0-stringObject.length-1；
              无整数值：从字符串的最后一个位置开始检索；
               没有查找到指定的字符，返回-1;
              }
              
    如：var  string = "hello world!"
    
       string.lastIndexOf("hello")  //0
       
       string.lastIndexOf("world")  //6
       
       string.lastIndexxOf("World")  //-1
       
       
  2、browser对象的集合
  
    { window   screen    history   location   navigator}
    
    (1）、window:表示浏览器中打开的窗口；
     
   （2）、screen：每个 Window 对象的 screen 属性都引用一个 Screen 对象。Screen 对象中存放着有关显示浏览器屏幕的信息
   
   （3）、 history ：History 对象包含用户（在浏览器窗口中）访问过的 URL，(window.history)
    
    (4)、  Navigator ：包含有关浏览器的信息。
   
